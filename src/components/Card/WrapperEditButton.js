import styled from 'styled-components';
import PropTypes from 'prop-types';

const WrapperEditButton = styled.div`
  position: absolute;
  top: 0;
  right: 0;
  padding-top: 5px;
  padding-right: 5px;
`;

WrapperEditButton.propTypes = {
};

export default WrapperEditButton;
