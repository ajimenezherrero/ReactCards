import styled from 'styled-components';
import PropTypes from 'prop-types';

const WrapperDescription = styled.div`
  padding: 15px;
  flex: 1;
`;

WrapperDescription.propTypes = {
};

export default WrapperDescription;
