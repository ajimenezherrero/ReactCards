import React from 'react';
import PropTypes from 'prop-types';

const CardsEmptyView = () => {
  return (
    <div>
      <label>EMPTY VIEW</label>
    </div>
  );
};

CardsEmptyView.propTypes = {
};

export default CardsEmptyView;
